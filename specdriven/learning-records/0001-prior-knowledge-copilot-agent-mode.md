# Prior knowledge: Copilot agent mode, no Spec Kit yet

Markus has used Copilot agent mode in VS Code for multi-file tasks, but has not used Spec Kit, the `specify` CLI, or the Copilot CLI, and has not followed a structured spec-driven workflow. Day job is Spring Boot + Angular with OpenAPI-generated clients, so API-contract thinking is already strong — lessons can build on that instead of teaching it.

**Implications:** Skip prompting basics and agent-mode orientation. The zone of proximal development starts at the SDD workflow itself and the Spec Kit toolchain. OpenAPI knowledge is an anchor to attach new concepts to (e.g. "a feature spec is to the implementation what an OpenAPI spec is to a client").
